package ch13_Interfaces.service;

//enum : enumeration의 약자.
//어떤 클래스가 상수로만 이뤄져 있을 때, class로 선언하지 않아도 된다.
// enum으로 선언하면 이 객체는 상수의 집합이라는 것을 명시적으로 나타낸다.
// 상수들의 타입을 지정할 필요가 없다. 이름만 써주면 된다. 
// 값의 초기화는 ....  Timevalue2 클래스에서 
public enum TimeValues {
	WEEK_THREE_HOUR,
	WEEKEND_SIX_HOUR;
}
